import { StorageCountPipe } from './storage-count.pipe';

describe('StorageCountPipe', () => {
  it('create an instance', () => {
    const pipe = new StorageCountPipe();
    expect(pipe).toBeTruthy();
  });
});
